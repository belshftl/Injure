[package]
name = "injureaudio"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[profile.dev]
panic = "abort"

[profile.release]
lto = true
panic = "abort"

[dependencies]
libc = "0.2"
num_enum = "0.7"
rtrb = "0.3"
